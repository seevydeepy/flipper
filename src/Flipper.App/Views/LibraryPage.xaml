<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Flipper.App.Views.LibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Flipper.App.Views"
    Background="{ThemeResource LayerFillColorDefaultBrush}">
    <Grid Padding="20" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Flipper" Style="{ThemeResource TitleTextBlockStyle}" VerticalAlignment="Center" />
            <TextBox x:Name="SearchBox" Grid.Column="1" PlaceholderText="Search" TextChanged="SearchBox_TextChanged" />
            <ComboBox x:Name="SortBox" Grid.Column="2" MinWidth="160" SelectionChanged="SortBox_SelectionChanged">
                <ComboBoxItem Content="A–Z" Tag="Name" />
                <ComboBoxItem Content="Recent" Tag="Recent" />
                <ComboBoxItem Content="Most played" Tag="MostPlayed" />
                <ComboBoxItem Content="Favourites" Tag="Favourites" />
            </ComboBox>
            <Button x:Name="SortDirectionButton" Grid.Column="3" Click="SortDirection_Click" AutomationProperties.Name="Reverse sort">
                <FontIcon x:Name="SortDirectionIcon" Glyph="&#xE74B;" FontSize="16" />
            </Button>
            <Button x:Name="ChooseFolderButton" Grid.Column="4" Content="Folder" Click="ChooseFolder_Click" />
        </Grid>

        <TextBlock x:Name="OfflineLabel" Grid.Row="1" Text="Library offline" Visibility="Collapsed" />

        <Grid Grid.Row="2" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ListView x:Name="FolderList" SelectionChanged="FolderList_SelectionChanged" />
            <GridView
                x:Name="ScoreGrid"
                Grid.Column="1"
                IsItemClickEnabled="True"
                ItemClick="ScoreGrid_ItemClick"
                SelectionMode="None">
                <GridView.ItemContainerTransitions>
                    <TransitionCollection>
                        <EntranceThemeTransition />
                        <AddDeleteThemeTransition />
                        <RepositionThemeTransition />
                    </TransitionCollection>
                </GridView.ItemContainerTransitions>
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="local:ScoreCard">
                        <Grid Width="176" Height="248" Padding="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="12">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Border Background="#E8E0D4" CornerRadius="8" Margin="0,0,0,8">
                                <Image Source="{x:Bind Preview, Mode=OneWay}" Stretch="UniformToFill" />
                            </Border>
                            <TextBlock Grid.Row="1" Text="{x:Bind Title}" TextTrimming="CharacterEllipsis" />
                            <Grid Grid.Row="2">
                                <TextBlock Text="{x:Bind Folder}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                <Button
                                    Style="{StaticResource QuietButton}"
                                    HorizontalAlignment="Right"
                                    Click="Favourite_Click"
                                    Tag="{x:Bind}">
                                    <FontIcon
                                        Glyph="{x:Bind StarGlyph, Mode=OneWay}"
                                        Foreground="{x:Bind StarBrush, Mode=OneWay}"
                                        FontSize="16" />
                                </Button>
                            </Grid>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </Grid>

        <TextBlock x:Name="ErrorLabel" Grid.Row="3" Visibility="Collapsed" />
    </Grid>
</Page>

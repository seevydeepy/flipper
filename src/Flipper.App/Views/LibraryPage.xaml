<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Flipper.App.Views.LibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Button x:Name="ChooseFolderButton" Content="Choose library folder" Click="ChooseFolder_Click" />
        <TextBox x:Name="SearchBox" Grid.Row="1" TextChanged="SearchBox_TextChanged" Visibility="Collapsed" />
        <TextBlock x:Name="OfflineLabel" Grid.Row="2" Text="Library offline" Visibility="Collapsed" />
        <Grid Grid.Row="3" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ListView x:Name="FolderList" SelectionChanged="FolderList_SelectionChanged" />
            <ListView x:Name="ScoreList" Grid.Column="1" IsItemClickEnabled="True" ItemClick="ScoreList_ItemClick" />
        </Grid>
        <TextBlock x:Name="ErrorLabel" Grid.Row="4" Visibility="Collapsed" />
    </Grid>
</Page>
